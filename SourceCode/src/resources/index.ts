/**
 * Resources Module Exports
 */

export { ResourceLoader, resourceLoader } from './loader';
export type {
  AIResourcesConfig,
  ResourceMetadata,
  ResourceType,
  ResourceSource,
  ResourceConflict,
  LoaderStats,
} from '../types/resources';
